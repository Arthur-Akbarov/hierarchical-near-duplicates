(T|t)he strings? .*resides? in memory assigned to the BerElement,? and must not be freed by the caller\.
    0 0 the string will reside in memory assigned to the BerElement, and must not be freed by the caller\.
    0 0 The string resides in memory assigned to the BerElement, and must not be freed by the caller\.
    0 0 The strings in each struct berval reside in memory assigned to the BerElement and must not be freed by the caller\.
